# Physim

A 2D physics simulation and visualization framework for TypeScript.

## Project Structure

The project is divided into two main parts:

- `core/`: Contains the core simulation runner, web interface, and build scripts.
- `std/`: The standard library that provides the building blocks for simulations, including modules for physics, math, and rendering.
